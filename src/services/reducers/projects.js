import { siteProjects } from "../../utils/projects";
const initialState = siteProjects;

const projectsList = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
  export default projectsList;